scalar Upload

type Info {
  id: ID!
  name: String!
}

type Query {
  user: User
  users: [Info!]!
  userAvatar(userUuid: ID!): String!
}

type Mutation {
  uploadAvatar(image: Upload!, userUuid: String!): String!
}
